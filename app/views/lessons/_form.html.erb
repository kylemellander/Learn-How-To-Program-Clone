<%= render 'layouts/errors', object: @lesson %>

<%= form_for @lesson do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>

  <%= f.label :content %>
  <%= f.text_area :content %>

  <%= f.label :sort_order %>
  <%= f.text_field :sort_order %>

  <%= f.submit %>
<% end %>
